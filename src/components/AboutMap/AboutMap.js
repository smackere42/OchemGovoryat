import styles from './styles.module.scss';

const AboutMap = () => {

    return(
        <>
            <div id='aboutToggle' className={styles.AboutMap}>
            </div>
        </>
    )
}

export default AboutMap;